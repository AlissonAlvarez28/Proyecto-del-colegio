/**
 * config.js — Un solo lugar para la dirección del backend.
 *
 * En desarrollo local, apunta a tu servidor corriendo en tu computadora.
 * Cuando despliegues el backend (por ejemplo en Render), cambia SOLO esta
 * línea — todas las páginas del sitio y el panel administrativo la leen
 * de aquí, no hace falta tocar ningún otro archivo.
 *
 * Ejemplo después de desplegar en Render:
 *   window.IGTFM_API_URL = 'https://igtfm-api.onrender.com/api';
 */
window.IGTFM_API_URL = 'http://localhost:3000/api';
